const ABOUT = {
  name: 'About',
  slug: '/about',
};

const PROJECTS = {
  name: 'Projects',
  slug: '/projects',
};

const BLOGS = {
  name: 'Blogs',
  slug: '/blogs',
};

const SNIPPETS = {
  name: 'Snippets',
  slug: '/snippets',
};

const RESUME = {
  name: 'Resume',
  slug: '/resume',
  highlight: true,
};

const CONTACT = {
  name: 'Contact',
  slug: '/contact',
};

const PAGES = [ABOUT, PROJECTS, BLOGS, SNIPPETS, RESUME, CONTACT];

export default PAGES;
